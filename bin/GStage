#!/usr/bin/perl
#
# GStage: a ridiculously (and intentionally) buggy IRC server
# bin/GStage: Starts up the Server instance
#
# Copyright (c) 2010 Stephen Belcher (sycobuny)
#

use warnings;
use strict;

use lib qw(lib);
use Object; # put it here so we don't have to anywhere else
use Server;

$0 = 'GStage';
my ($server) = Server->new('GStage', 'local');

exit 0;
